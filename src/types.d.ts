export interface CardProps {
  number: number | string;
  text: string;
}
export interface InfoCardProps {
  title: string;
  text: string;
}
export interface IProps {
  text: string;
  lat: number;
  lng: number;
}
